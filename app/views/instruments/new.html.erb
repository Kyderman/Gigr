
<div class="jumbotron">
	<h1>Create New Instrument</h1>
</div>
<div class="well">
	<%= render 'form' %>
	<% if current_user.has_role? :admin%>
		<%= link_to 'Back', instruments_path, :class => "btn btn-danger" %>
	<%end%>
	</div>
</div>

<%= render "layouts/footer"%>