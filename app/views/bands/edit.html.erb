
<div class="jumbotron">
	<h1>Edit Band Profile</h1>
</div>
<div class ="well">
	<div class="row">
		<div class="col-sm-12">	
			<%= render 'form' %>
			<%= link_to 'Exit Edit', @band, :class => "btn btn-info" %>
			<% if current_user.has_role? :admin%>
				<%= link_to 'Back', bands_path, :class => "btn btn-danger" %>
			<%end%>
			</div>
		</div>
	</div>
</div>
<%= render "layouts/footer"%>
